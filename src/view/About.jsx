import React, { useEffect } from 'react';

const About = ({ meta }) => {
  console.log('about render', 'width meta: ', meta);
  return (
    <div>
      <div>about</div>
    </div>
  );
};

export default About;
